name: Release

on:
  push:
    tags:
      - v[0-9]+*

permissions:
  contents: read

jobs:
  release:
    permissions:
      contents: write
    uses: go-openapi/ci-workflows/.github/workflows/release.yml@master
    with:
      tag: ${{ github.ref_name }}
    secrets: inherit
