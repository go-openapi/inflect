name: Dependabot auto-merge
on: pull_request

permissions:
  contents: write
  pull-requests: write

jobs:
  dependabot:
    uses: go-openapi/ci-workflows/.github/workflows/auto-merge.yml@master
    secrets: inherit
